meta {
  name: delete-external-songs-id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/external/songs/{{sample_id}}
  auth: bearer
}

auth:bearer {
  token: {{API_KEY}}
}
docs {
  Domain external [orphan]. Auth api-key.
}
