meta {
  name: get-lessons-stats-advanced
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/lessons/stats/advanced
  auth: inherit
}

tests {
  test('Status 200', function() {
    expect(res.status).to.equal(200);
  });
}