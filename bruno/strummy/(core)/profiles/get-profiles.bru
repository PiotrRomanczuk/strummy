meta {
  name: get-profiles
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/profiles
  auth: inherit
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("response is array", function() {
    expect(res.body).to.be.an("array");
  });
}

docs {
  Domain profiles. Auth user, role:admin+teacher.
}
