meta {
  name: post-admin-set-passwords
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/admin/set-passwords
  body: json
  auth: bearer
}

auth:bearer {
  token: {{SUPABASE_SERVICE_ROLE_KEY}}
}
body { json }

body:json {
  {}
}
docs {
  Domain admin [orphan]. Auth service-role.
}
