meta {
  name: get-song-admin-favorites
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/song/admin-favorites
  auth: inherit
}

tests {
  test('Status 200', function() {
    expect(res.status).to.equal(200);
  });
  test('Response is an array', function() {
    expect(res.body).to.be.an('array');
  });
}