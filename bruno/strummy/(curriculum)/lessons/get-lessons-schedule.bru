meta {
  name: get-lessons-schedule
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/lessons/schedule?teacherId={{sample_teacher_id}}
  auth: inherit
}

tests {
  test('Status 200', function() {
    expect(res.status).to.equal(200);
  });
}
