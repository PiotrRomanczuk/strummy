meta {
  name: delete-lessons-id
  type: http
  seq: 99
}

delete {
  url: {{baseUrl}}/api/lessons/{{created_lesson_id}}
  auth: inherit
}

tests {
  test('Status 200 or 404', function() {
    expect([200, 204, 404]).to.include(res.status);
  });
}