meta {
  name: get-song-stats-advanced
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/api/song/stats/advanced
  auth: inherit
}

tests {
  test('Status 200 or 403', function() {
    expect([200, 403]).to.include(res.status);
  });
}