meta {
  name: delete-song-bulk
  type: http
  seq: 98
}

delete {
  url: {{baseUrl}}/api/song/bulk
  body: json
  auth: inherit
}

body:json {
  {
    "ids": []
  }
}

tests {
  test('Status 400 for empty ids', function() {
    expect([400, 422]).to.include(res.status);
  });
}